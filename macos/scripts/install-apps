#!/usr/bin/env bash

set -euo pipefail

if ! command -v brew >/dev/null 2>&1; then
	printf 'install-apps: the selected Tart base image must provide Homebrew\n' >&2
	exit 1
fi

brew update
brew install --cask firefox
brew install --cask ghostty
brew install neovim
brew install dockutil
